import { ToastProvider, ToastViewport, toast, Button, Badge } from "@/index";
import { ComponentPreview } from "@docs/components/ComponentPreview";
import { CodeBlock } from "@docs/components/CodeBlock";
import { Bell, CheckCircle2, AlertTriangle, AlertCircle } from "lucide-react";

# Toast

A succinct notification that floats temporarily over the screen to communicate quick feedback. Built on Base UI Toast with auto-dismiss and swipe gestures.

<div className="flex flex-wrap items-center gap-2 my-4">
  <Badge variant="outline" className="font-mono text-[11px]">@base-ui/react</Badge>
  <Badge variant="secondary" className="font-mono text-[11px]">Auto Dismiss</Badge>
</div>

## Import

<CodeBlock language="tsx" code={'import { ToastProvider, ToastViewport, toast } from "@galaui/react";'} />

## Example

<ComponentPreview
  title="Toast Notifications"
  description="Click buttons below to trigger stacked toasts."
  code={'import { toast, Button } from "@galaui/react";\nimport { Bell, CheckCircle2, AlertTriangle, AlertCircle } from "lucide-react";\n\nexport default function Example() {\n  return (\n    <div className="flex flex-wrap gap-2.5 items-center">\n      <Button\n        variant="default"\n        size="sm"\n        onClick={() => toast.show("System update installed", "GalaUI v0.1.2 is active.")}\n      >\n        <Bell className="w-3.5 h-3.5 mr-1.5" />\n        Default\n      </Button>\n      <Button\n        variant="outline"\n        size="sm"\n        onClick={() => toast.success("Token Synced", "All variables updated.")}\n      >\n        <CheckCircle2 className="w-3.5 h-3.5 mr-1.5 text-emerald-600" />\n        Success\n      </Button>\n      <Button\n        variant="destructive"\n        size="sm"\n        onClick={() => toast.error("Cluster Offline", "Could not connect to host.")}\n      >\n        <AlertCircle className="w-3.5 h-3.5 mr-1.5" />\n        Destructive\n      </Button>\n    </div>\n  );\n}'}
>
  <div className="flex flex-wrap gap-2.5 items-center">
    <Button
      variant="default"
      size="sm"
      onClick={() => toast.show("System update installed", "GalaUI v0.1.2 is active.")}
    >
      <Bell className="w-3.5 h-3.5 mr-1.5" />
      Default
    </Button>
    <Button
      variant="outline"
      size="sm"
      onClick={() => toast.success("Token Synced", "All variables updated.")}
    >
      <CheckCircle2 className="w-3.5 h-3.5 mr-1.5 text-emerald-600" />
      Success
    </Button>
    <Button
      variant="destructive"
      size="sm"
      onClick={() => toast.error("Cluster Offline", "Could not connect to host.")}
    >
      <AlertCircle className="w-3.5 h-3.5 mr-1.5" />
      Destructive
    </Button>
  </div>
</ComponentPreview>
