import { Button } from "@/index";
import { ComponentPreview } from "@docs/components/ComponentPreview";
import { ArrowRight, Trash2, Heart, Download } from "lucide-react";

# Button

Displays a button or a component that looks like a button. Built with customizable CVA variants and Base UI interactive primitives.

---

## Default Showcase

<ComponentPreview
  title="Variants"
  description="Different visual emphasis and contextual styles."
  code={'<div className="flex flex-wrap gap-2.5 items-center">\n  <Button variant="default">Primary</Button>\n  <Button variant="secondary">Secondary</Button>\n  <Button variant="outline">Outline</Button>\n  <Button variant="destructive">Destructive</Button>\n  <Button variant="ghost">Ghost</Button>\n  <Button variant="link">Link Style</Button>\n</div>'}
>
  <div className="flex flex-wrap gap-2.5 items-center">
    <Button variant="default">Primary</Button>
    <Button variant="secondary">Secondary</Button>
    <Button variant="outline">Outline</Button>
    <Button variant="destructive">Destructive</Button>
    <Button variant="ghost">Ghost</Button>
    <Button variant="link">Link Style</Button>
  </div>
</ComponentPreview>

---

## Sizes

<ComponentPreview
  title="Size Scale"
  description="Available sizes from xs, sm, md, to lg."
  code={'<div className="flex flex-wrap items-center gap-2.5">\n  <Button size="xs">Extra Small (xs)</Button>\n  <Button size="sm">Small (sm)</Button>\n  <Button size="default">Default / Medium (md)</Button>\n  <Button size="lg">Large (lg)</Button>\n</div>'}
>
  <div className="flex flex-wrap items-center gap-2.5">
    <Button size="xs">Extra Small (xs)</Button>
    <Button size="sm">Small (sm)</Button>
    <Button size="default">Default / Medium (md)</Button>
    <Button size="lg">Large (lg)</Button>
  </div>
</ComponentPreview>

---

## With Icons

<ComponentPreview
  title="Icons & Actions"
  description="Buttons with leading, trailing, or icon-only layouts."
  code={'<div className="flex flex-wrap items-center gap-2.5">\n  <Button variant="default">\n    Continue\n    <ArrowRight className="w-4 h-4 ml-1.5" />\n  </Button>\n  <Button variant="outline">\n    <Download className="w-4 h-4 mr-1.5" />\n    Download\n  </Button>\n  <Button variant="destructive" size="icon">\n    <Trash2 className="w-4 h-4" />\n  </Button>\n  <Button variant="secondary" size="icon">\n    <Heart className="w-4 h-4 text-red-500 fill-red-500" />\n  </Button>\n</div>'}
>
  <div className="flex flex-wrap items-center gap-2.5">
    <Button variant="default">
      Continue
      <ArrowRight className="w-4 h-4 ml-1.5" />
    </Button>
    <Button variant="outline">
      <Download className="w-4 h-4 mr-1.5" />
      Download
    </Button>
    <Button variant="destructive" size="icon">
      <Trash2 className="w-4 h-4" />
    </Button>
    <Button variant="secondary" size="icon">
      <Heart className="w-4 h-4 text-red-500 fill-red-500" />
    </Button>
  </div>
</ComponentPreview>

---

## Disabled State

<ComponentPreview
  title="Disabled"
  description="Disabled interactive state."
  code={'<div className="flex gap-2">\n  <Button disabled variant="default">Disabled Primary</Button>\n  <Button disabled variant="outline">Disabled Outline</Button>\n</div>'}
>
  <div className="flex gap-2">
    <Button disabled variant="default">Disabled Primary</Button>
    <Button disabled variant="outline">Disabled Outline</Button>
  </div>
</ComponentPreview>

