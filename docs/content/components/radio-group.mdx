import { RadioGroup, RadioGroupItem, Label, Badge } from "@/index";
import { ComponentPreview } from "@docs/components/ComponentPreview";
import { CodeBlock } from "@docs/components/CodeBlock";
import { ApiReference } from "@docs/components/ApiReference";

# Radio Group

A set of checkable buttons—known as radio buttons—where no more than one button can be checked at a time. Built on Base UI RadioGroup primitives with arrow-key traversal.

<div className="flex flex-wrap items-center gap-2 my-4">
  <Badge variant="outline" className="font-mono text-[11px]">@base-ui/react</Badge>
  <Badge variant="secondary" className="font-mono text-[11px]">Keyboard Traversal</Badge>
</div>

## Import

<CodeBlock language="tsx" code={'import { RadioGroup, RadioGroupItem, Label } from "@galaui/react";'} />

## Example

<ComponentPreview
  title="Subscription Plan"
  description="Mutually exclusive radio choices."
  code={'import { RadioGroup, RadioGroupItem, Label } from "@galaui/react";\n\nexport default function Example() {\n  return (\n    <RadioGroup defaultValue="pro" className="space-y-2.5 max-w-sm">\n      <div className="flex items-center gap-2.5">\n        <RadioGroupItem value="starter" id="r-starter" />\n        <Label htmlFor="r-starter">Starter Plan (Free)</Label>\n      </div>\n      <div className="flex items-center gap-2.5">\n        <RadioGroupItem value="pro" id="r-pro" />\n        <Label htmlFor="r-pro">Professional Tier ($19/mo)</Label>\n      </div>\n      <div className="flex items-center gap-2.5">\n        <RadioGroupItem value="enterprise" id="r-enterprise" />\n        <Label htmlFor="r-enterprise">Enterprise Cluster (Custom)</Label>\n      </div>\n    </RadioGroup>\n  );\n}'}
>
  <RadioGroup defaultValue="pro" className="space-y-2.5 max-w-sm">
    <div className="flex items-center gap-2.5">
      <RadioGroupItem value="starter" id="r-starter" />
      <Label htmlFor="r-starter">
        Starter Plan (Free)
      </Label>
    </div>
    <div className="flex items-center gap-2.5">
      <RadioGroupItem value="pro" id="r-pro" />
      <Label htmlFor="r-pro">
        Professional Tier ($19/mo)
      </Label>
    </div>
    <div className="flex items-center gap-2.5">
      <RadioGroupItem value="enterprise" id="r-enterprise" />
      <Label htmlFor="r-enterprise">
        Enterprise Cluster (Custom)
      </Label>
    </div>
  </RadioGroup>
</ComponentPreview>

## API Reference

<ApiReference component="RadioGroup" />
