import { Button, Tooltip, TooltipTrigger, TooltipPopup, Popover, PopoverTrigger, PopoverPopup, PopoverTitle, PopoverDescription, DropdownMenu, DropdownMenuTrigger, DropdownMenuPopup, DropdownMenuItem, DropdownMenuSeparator, Badge } from "@/index";
import { ComponentPreview } from "@docs/components/ComponentPreview";
import { CodeBlock } from "@docs/components/CodeBlock";
import { MoreHorizontal, User, Settings, LogOut, Info, Bell, Shield, Sliders } from "lucide-react";

# Overlays & Tooltips

Floating context elements built on Base UI floating primitives with auto-positioning, collision detection, and smooth open/close micro-animations.

<div className="flex flex-wrap items-center gap-2 my-4">
  <Badge variant="outline" className="font-mono text-[11px]">@base-ui/react</Badge>
  <Badge variant="secondary" className="font-mono text-[11px]">WAI-ARIA Compliant</Badge>
  <Badge variant="success" className="font-mono text-[11px]">Collision Aware</Badge>
</div>

## Import

<CodeBlock
  language="tsx"
  code={'import {\n  Tooltip,\n  TooltipTrigger,\n  TooltipPopup,\n  Popover,\n  PopoverTrigger,\n  PopoverPopup,\n  PopoverTitle,\n  PopoverDescription,\n  DropdownMenu,\n  DropdownMenuTrigger,\n  DropdownMenuPopup,\n  DropdownMenuItem,\n  DropdownMenuSeparator,\n} from "@galaui/react";'}
/>

---

## Tooltip

Displays helpful contextual information when users hover or focus on an interactive element.

<ComponentPreview
  title="Hover Tooltip"
  description="Tooltip with automated collision boundaries and smooth entry transition."
  code={'import { Tooltip, TooltipTrigger, TooltipPopup, Button } from "@galaui/react";\nimport { Info, Bell } from "lucide-react";\n\nexport default function Example() {\n  return (\n    <div className="flex items-center gap-4">\n      <Tooltip>\n        <TooltipTrigger>\n          <Button variant="outline" size="sm">\n            <Info className="w-4 h-4 mr-1.5 text-primary" />\n            Hover for info\n          </Button>\n        </TooltipTrigger>\n        <TooltipPopup>\n          GalaUI Tooltip with Base UI collision detection\n        </TooltipPopup>\n      </Tooltip>\n\n      <Tooltip>\n        <TooltipTrigger>\n          <Button variant="secondary" size="icon">\n            <Bell className="w-4 h-4" />\n          </Button>\n        </TooltipTrigger>\n        <TooltipPopup>\n          Notification settings (3 unread)\n        </TooltipPopup>\n      </Tooltip>\n    </div>\n  );\n}'}
>
  <div className="flex items-center gap-4">
    <Tooltip>
      <TooltipTrigger>
        <Button variant="outline" size="sm">
          <Info className="w-4 h-4 mr-1.5 text-primary" />
          Hover for info
        </Button>
      </TooltipTrigger>
      <TooltipPopup>
        GalaUI Tooltip with Base UI collision detection
      </TooltipPopup>
    </Tooltip>

    <Tooltip>
      <TooltipTrigger>
        <Button variant="secondary" size="icon">
          <Bell className="w-4 h-4" />
        </Button>
      </TooltipTrigger>
      <TooltipPopup>
        Notification settings (3 unread)
      </TooltipPopup>
    </Tooltip>
  </div>
</ComponentPreview>

---

## Popover

A rich floating card opened by clicking an anchor button. Useful for micro-forms, dimension selectors, or contextual settings.

<ComponentPreview
  title="Interactive Popover"
  description="Click trigger button to toggle the popover panel."
  code={'import {\n  Popover,\n  PopoverTrigger,\n  PopoverPopup,\n  PopoverTitle,\n  PopoverDescription,\n  Button,\n} from "@galaui/react";\nimport { Sliders } from "lucide-react";\n\nexport default function Example() {\n  return (\n    <Popover>\n      <PopoverTrigger>\n        <Button variant="default" size="sm">\n          <Sliders className="w-4 h-4 mr-1.5" />\n          Configure Dimensions\n        </Button>\n      </PopoverTrigger>\n      <PopoverPopup className="p-4 w-76 space-y-3">\n        <div className="space-y-1">\n          <PopoverTitle className="font-semibold text-sm">Canvas Configuration</PopoverTitle>\n          <PopoverDescription className="text-xs text-muted-foreground">\n            Adjust default canvas width and height for export.\n          </PopoverDescription>\n        </div>\n        <div className="grid grid-cols-2 gap-2 text-xs">\n          <div className="space-y-1">\n            <span className="font-medium text-muted-foreground">Width</span>\n            <input className="w-full px-2.5 py-1 rounded-md border border-border bg-muted/40 font-mono text-xs" defaultValue="1440px" />\n          </div>\n          <div className="space-y-1">\n            <span className="font-medium text-muted-foreground">Height</span>\n            <input className="w-full px-2.5 py-1 rounded-md border border-border bg-muted/40 font-mono text-xs" defaultValue="900px" />\n          </div>\n        </div>\n      </PopoverPopup>\n    </Popover>\n  );\n}'}
>
  <Popover>
    <PopoverTrigger>
      <Button variant="default" size="sm">
        <Sliders className="w-4 h-4 mr-1.5" />
        Configure Dimensions
      </Button>
    </PopoverTrigger>
    <PopoverPopup className="p-4 w-76 space-y-3">
      <div className="space-y-1">
        <PopoverTitle className="font-semibold text-sm">Canvas Configuration</PopoverTitle>
        <PopoverDescription className="text-xs text-muted-foreground">
          Adjust default canvas width and height for export.
        </PopoverDescription>
      </div>
      <div className="grid grid-cols-2 gap-2 text-xs">
        <div className="space-y-1">
          <span className="font-medium text-muted-foreground">Width</span>
          <input className="w-full px-2.5 py-1 rounded-md border border-border bg-muted/40 font-mono text-xs" defaultValue="1440px" />
        </div>
        <div className="space-y-1">
          <span className="font-medium text-muted-foreground">Height</span>
          <input className="w-full px-2.5 py-1 rounded-md border border-border bg-muted/40 font-mono text-xs" defaultValue="900px" />
        </div>
      </div>
    </PopoverPopup>
  </Popover>
</ComponentPreview>

---

## Dropdown Menu

Displays a menu to the user - such as a set of actions or functions - triggered by a button.

<ComponentPreview
  title="Action Menu"
  description="Context menu with keyboard navigation, hover states, and destructive options."
  code={'import {\n  DropdownMenu,\n  DropdownMenuTrigger,\n  DropdownMenuPopup,\n  DropdownMenuItem,\n  DropdownMenuSeparator,\n  Button,\n} from "@galaui/react";\nimport { MoreHorizontal, User, Settings, Shield, LogOut } from "lucide-react";\n\nexport default function Example() {\n  return (\n    <DropdownMenu>\n      <DropdownMenuTrigger>\n        <Button variant="outline" size="sm">\n          Options Menu\n          <MoreHorizontal className="w-4 h-4 ml-1.5 text-muted-foreground" />\n        </Button>\n      </DropdownMenuTrigger>\n      <DropdownMenuPopup className="w-52">\n        <DropdownMenuItem className="gap-2.5">\n          <User className="w-4 h-4 text-muted-foreground" />\n          <span>Account Profile</span>\n        </DropdownMenuItem>\n        <DropdownMenuItem className="gap-2.5">\n          <Settings className="w-4 h-4 text-muted-foreground" />\n          <span>Team Preferences</span>\n        </DropdownMenuItem>\n        <DropdownMenuItem className="gap-2.5">\n          <Shield className="w-4 h-4 text-muted-foreground" />\n          <span>Security & 2FA</span>\n        </DropdownMenuItem>\n        <DropdownMenuSeparator />\n        <DropdownMenuItem className="gap-2.5 text-destructive">\n          <LogOut className="w-4 h-4" />\n          <span>Sign Out</span>\n        </DropdownMenuItem>\n      </DropdownMenuPopup>\n    </DropdownMenu>\n  );\n}'}
>
  <DropdownMenu>
    <DropdownMenuTrigger>
      <Button variant="outline" size="sm">
        Options Menu
        <MoreHorizontal className="w-4 h-4 ml-1.5 text-muted-foreground" />
      </Button>
    </DropdownMenuTrigger>
    <DropdownMenuPopup className="w-52">
      <DropdownMenuItem className="gap-2.5">
        <User className="w-4 h-4 text-muted-foreground" />
        <span>Account Profile</span>
      </DropdownMenuItem>
      <DropdownMenuItem className="gap-2.5">
        <Settings className="w-4 h-4 text-muted-foreground" />
        <span>Team Preferences</span>
      </DropdownMenuItem>
      <DropdownMenuItem className="gap-2.5">
        <Shield className="w-4 h-4 text-muted-foreground" />
        <span>Security & 2FA</span>
      </DropdownMenuItem>
      <DropdownMenuSeparator />
      <DropdownMenuItem className="gap-2.5 text-destructive">
        <LogOut className="w-4 h-4" />
        <span>Sign Out</span>
      </DropdownMenuItem>
    </DropdownMenuPopup>
  </DropdownMenu>
</ComponentPreview>

---

## Component Anatomy

Overview of exported subcomponents:

### Tooltip Parts
| Component | Role |
| :--- | :--- |
| `Tooltip` | Root container managing open/close hover timers. |
| `TooltipTrigger` | The anchor element that receives pointer and keyboard focus. |
| `TooltipPopup` | The animated floating tooltip card. |

### Popover Parts
| Component | Role |
| :--- | :--- |
| `Popover` | Root state controller for the popover surface. |
| `PopoverTrigger` | Button element that toggles visibility on click. |
| `PopoverPopup` | Floating surface container with elevation and arrow alignment. |
| `PopoverTitle` | Accessible title for assistive technologies. |
| `PopoverDescription` | Accessible description text. |

