import { Checkbox, CheckboxGroup, Label, Badge } from "@/index";
import { ComponentPreview } from "@docs/components/ComponentPreview";
import { CodeBlock } from "@docs/components/CodeBlock";
import { ApiReference } from "@docs/components/ApiReference";

# Checkbox

A control that allows the user to toggle between checked and unchecked states. Built on Base UI Checkbox primitives with keyboard accessibility.

<div className="flex flex-wrap items-center gap-2 my-4">
  <Badge variant="outline" className="font-mono text-[11px]">@base-ui/react</Badge>
  <Badge variant="secondary" className="font-mono text-[11px]">Tri-State Ready</Badge>
</div>

## Import

<CodeBlock language="tsx" code={'import { Checkbox, CheckboxGroup, Label } from "@galaui/react";'} />

## Single & Group

<ComponentPreview
  title="Checkbox Group"
  description="Multiple selection checkboxes with labels and helper text."
  code={'import { Checkbox, CheckboxGroup, Label } from "@galaui/react";\n\nexport default function Example() {\n  return (\n    <div className="space-y-4 max-w-sm">\n      <div className="flex items-center gap-2.5">\n        <Checkbox id="terms" defaultChecked />\n        <Label htmlFor="terms">Accept terms and conditions</Label>\n      </div>\n      <CheckboxGroup defaultValue={["email"]} className="space-y-2.5 pt-2 border-t border-border">\n        <div className="flex items-center gap-2.5">\n          <Checkbox value="email" id="email" />\n          <Label htmlFor="email" className="text-muted-foreground font-normal">\n            Email notifications\n          </Label>\n        </div>\n        <div className="flex items-center gap-2.5">\n          <Checkbox value="sms" id="sms" />\n          <Label htmlFor="sms" className="text-muted-foreground font-normal">\n            SMS security alerts\n          </Label>\n        </div>\n      </CheckboxGroup>\n    </div>\n  );\n}'}
>
  <div className="space-y-4 max-w-sm">
    <div className="flex items-center gap-2.5">
      <Checkbox id="terms" defaultChecked />
      <Label htmlFor="terms">
        Accept terms and conditions
      </Label>
    </div>
    <CheckboxGroup defaultValue={["email"]} className="space-y-2.5 pt-2 border-t border-border">
      <div className="flex items-center gap-2.5">
        <Checkbox value="email" id="email" />
        <Label htmlFor="email" className="text-muted-foreground font-normal">
          Email notifications
        </Label>
      </div>
      <div className="flex items-center gap-2.5">
        <Checkbox value="sms" id="sms" />
        <Label htmlFor="sms" className="text-muted-foreground font-normal">
          SMS security alerts
        </Label>
      </div>
    </CheckboxGroup>
  </div>
</ComponentPreview>

## API Reference

<ApiReference component="Checkbox" />
