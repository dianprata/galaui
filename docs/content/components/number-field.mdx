import {
  NumberField,
  NumberFieldGroup,
  NumberFieldInput,
  NumberFieldStepper,
  NumberFieldIncrement,
  NumberFieldDecrement,
  Badge,
} from "@/index";
import { ComponentPreview } from "@docs/components/ComponentPreview";
import { CodeBlock } from "@docs/components/CodeBlock";

# Number Field

An input field tailored for numeric values, complete with stepper controls and keyboard adjustments. Built on Base UI NumberField primitives.

<div className="flex flex-wrap items-center gap-2 my-4">
  <Badge variant="outline" className="font-mono text-[11px]">@base-ui/react</Badge>
  <Badge variant="secondary" className="font-mono text-[11px]">Arrow Increment</Badge>
</div>

## Import

<CodeBlock language="tsx" code={'import {\n  NumberField,\n  NumberFieldGroup,\n  NumberFieldInput,\n  NumberFieldStepper,\n  NumberFieldIncrement,\n  NumberFieldDecrement,\n} from "@galaui/react";'} />

## Example

<ComponentPreview
  title="Numeric Stepper"
  description="Quantity selector with up/down stepper buttons."
  code={'import {\n  NumberField,\n  NumberFieldGroup,\n  NumberFieldInput,\n  NumberFieldStepper,\n  NumberFieldIncrement,\n  NumberFieldDecrement,\n} from "@galaui/react";\n\nexport default function Example() {\n  return (\n    <div className="w-full max-w-xs space-y-1.5">\n      <label className="text-xs font-medium">Quantity</label>\n      <NumberField defaultValue={1} min={1} max={99}>\n        <NumberFieldGroup>\n          <NumberFieldInput />\n          <NumberFieldStepper>\n            <NumberFieldIncrement />\n            <NumberFieldDecrement />\n          </NumberFieldStepper>\n        </NumberFieldGroup>\n      </NumberField>\n    </div>\n  );\n}'}
>
  <div className="w-full max-w-xs space-y-1.5">
    <label className="text-xs font-medium">Quantity</label>
    <NumberField defaultValue={1} min={1} max={99}>
      <NumberFieldGroup>
        <NumberFieldInput />
        <NumberFieldStepper>
          <NumberFieldIncrement />
          <NumberFieldDecrement />
        </NumberFieldStepper>
      </NumberFieldGroup>
    </NumberField>
  </div>
</ComponentPreview>
