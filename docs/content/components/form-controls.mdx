import { Input, Textarea, Switch, Checkbox, Progress, Label } from "@/index";
import { ComponentPreview } from "@docs/components/ComponentPreview";
import { CodeBlock } from "@docs/components/CodeBlock";

# Form Controls

Core input controls including Text Input, Textarea, Toggle Switch, Checkbox, and Progress Indicators.

## Import

<CodeBlock language="tsx" code={'import { Input, Textarea, Switch, Checkbox, Progress, Label } from "@galaui/react";'} />

## Text Inputs & Textarea

<ComponentPreview
  title="Input Fields"
  description="Text inputs and multiline textareas."
  code={'import { Input, Textarea } from "@galaui/react";\n\nexport default function Example() {\n  return (\n    <div className="w-full max-w-sm space-y-4">\n      <div className="space-y-1.5">\n        <Label htmlFor="fc-user">Username</Label>\n        <Input id="fc-user" placeholder="Enter username..." />\n      </div>\n      <div className="space-y-1.5">\n        <Label htmlFor="fc-bio">Bio</Label>\n        <Textarea id="fc-bio" rows={3} placeholder="Tell us a little bit about yourself..." />\n      </div>\n    </div>\n  );\n}'}
>
  <div className="w-full max-w-sm space-y-4">
    <div className="space-y-1.5">
      <Label htmlFor="fc-user">Username</Label>
      <Input id="fc-user" placeholder="Enter username..." />
    </div>
    <div className="space-y-1.5">
      <Label htmlFor="fc-bio">Bio</Label>
      <Textarea id="fc-bio" rows={3} placeholder="Tell us a little bit about yourself..." />
    </div>
  </div>
</ComponentPreview>

## Switch & Checkbox

<ComponentPreview
  title="Toggle Controls"
  description="Switches and checkboxes with accessible keyboard states."
  code={'import { Switch, Checkbox, Label } from "@galaui/react";\n\nexport default function Example() {\n  return (\n    <div className="w-full max-w-sm space-y-3">\n      <div className="flex items-center justify-between p-3 rounded-lg border border-border">\n        <Label htmlFor="fc-email">Email Notifications</Label>\n        <Switch id="fc-email" defaultChecked />\n      </div>\n      <div className="flex items-center gap-2.5 px-1">\n        <Checkbox id="terms" defaultChecked />\n        <Label htmlFor="terms" className="font-normal text-muted-foreground">\n          I agree to the terms and privacy conditions\n        </Label>\n      </div>\n    </div>\n  );\n}'}
>
  <div className="w-full max-w-sm space-y-3">
    <div className="flex items-center justify-between p-3 rounded-lg border border-border">
      <Label htmlFor="fc-email">Email Notifications</Label>
      <Switch id="fc-email" defaultChecked />
    </div>
    <div className="flex items-center gap-2.5 px-1">
      <Checkbox id="terms" defaultChecked />
      <Label htmlFor="terms" className="font-normal text-muted-foreground">
        I agree to the terms and privacy conditions
      </Label>
    </div>
  </div>
</ComponentPreview>

## Progress Indicator

<ComponentPreview
  title="Progress Bar"
  description="Deterministic progress bar indicator."
  code={'import { Progress } from "@galaui/react";\n\nexport default function Example() {\n  return (\n    <div className="w-full max-w-sm space-y-2">\n      <div className="flex justify-between text-xs text-muted-foreground">\n        <span>Storage Capacity</span>\n        <span>68%</span>\n      </div>\n      <Progress value={68} />\n    </div>\n  );\n}'}
>
  <div className="w-full max-w-sm space-y-2">
    <div className="flex justify-between text-xs text-muted-foreground">
      <span>Storage Capacity</span>
      <span>68%</span>
    </div>
    <Progress value={68} />
  </div>
</ComponentPreview>
