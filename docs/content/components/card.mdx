import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
  Button,
  Input,
  Label,
  Select,
  SelectTrigger,
  SelectValue,
  SelectPopup,
  SelectItem,
} from "@/index";
import { ComponentPreview } from "@docs/components/ComponentPreview";
import { CodeBlock } from "@docs/components/CodeBlock";
import { ApiReference } from "@docs/components/ApiReference";

# Card

Displays a structured card container with header, content, and footer sections.

## Import

<CodeBlock language="tsx" code={'import {\n  Card,\n  CardHeader,\n  CardTitle,\n  CardDescription,\n  CardContent,\n  CardFooter,\n} from "@galaui/react";'} />

## Example

<ComponentPreview
  title="Interactive Card Container"
  description="Card component with title, body, and action footer."
  code={'import {\n  Card,\n  CardHeader,\n  CardTitle,\n  CardDescription,\n  CardContent,\n  CardFooter,\n  Button,\n  Input,\n  Label,\n  Select,\n  SelectTrigger,\n  SelectValue,\n  SelectPopup,\n  SelectItem,\n} from "@galaui/react";\n\nexport default function Example() {\n  return (\n    <Card className="w-full max-w-md">\n      <CardHeader>\n        <CardTitle>Create project</CardTitle>\n        <CardDescription>Deploy your new project in one-click.</CardDescription>\n      </CardHeader>\n      <CardContent className="space-y-4">\n        <div className="space-y-1.5">\n          <Label htmlFor="project-name">Project Name</Label>\n          <Input id="project-name" placeholder="my-awesome-app" />\n        </div>\n        <div className="space-y-1.5">\n          <Label htmlFor="framework">Framework</Label>\n          <Select defaultValue="next">\n            <SelectTrigger id="framework">\n              <SelectValue placeholder="Select a framework" />\n            </SelectTrigger>\n            <SelectPopup>\n              <SelectItem value="next">Next.js</SelectItem>\n              <SelectItem value="vite">Vite + React</SelectItem>\n              <SelectItem value="astro">Astro</SelectItem>\n              <SelectItem value="remix">Remix</SelectItem>\n            </SelectPopup>\n          </Select>\n        </div>\n      </CardContent>\n      <CardFooter className="flex justify-between items-center border-t border-border pt-4">\n        <Button variant="outline">Cancel</Button>\n        <Button variant="default">Deploy</Button>\n      </CardFooter>\n    </Card>\n  );\n}'}
>
  <Card className="w-full max-w-md">
    <CardHeader>
      <CardTitle>Create project</CardTitle>
      <CardDescription>Deploy your new project in one-click.</CardDescription>
    </CardHeader>
    <CardContent className="space-y-4">
      <div className="space-y-1.5">
        <Label htmlFor="project-name">Project Name</Label>
        <Input id="project-name" placeholder="my-awesome-app" />
      </div>
      <div className="space-y-1.5">
        <Label htmlFor="framework">Framework</Label>
        <Select defaultValue="next">
          <SelectTrigger id="framework">
            <SelectValue placeholder="Select a framework" />
          </SelectTrigger>
          <SelectPopup>
            <SelectItem value="next">Next.js</SelectItem>
            <SelectItem value="vite">Vite + React</SelectItem>
            <SelectItem value="astro">Astro</SelectItem>
            <SelectItem value="remix">Remix</SelectItem>
          </SelectPopup>
        </Select>
      </div>
    </CardContent>
    <CardFooter className="flex justify-between items-center border-t border-border pt-4">
      <Button variant="outline">Cancel</Button>
      <Button variant="default">Deploy</Button>
    </CardFooter>
  </Card>
</ComponentPreview>

## API Reference

<ApiReference component="Card" />
