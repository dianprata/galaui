import { Badge } from "@/index";
import { ComponentPreview } from "@docs/components/ComponentPreview";
import { CodeBlock } from "@docs/components/CodeBlock";
import { Sparkles, AlertTriangle, CheckCircle2 } from "lucide-react";
import { ApiReference } from "@docs/components/ApiReference";

# Badge

Displays a small status label, tag, or counter.

## Import

<CodeBlock language="tsx" code={'import { Badge } from "@galaui/react";'} />

## Variants

<ComponentPreview
  title="Semantic Badges"
  description="Status indicators and contextual color variants."
  code={'import { Badge } from "@galaui/react";\n\nexport default function Example() {\n  return (\n    <div className="flex flex-wrap gap-2 items-center">\n      <Badge variant="default">Default</Badge>\n      <Badge variant="secondary">Secondary</Badge>\n      <Badge variant="outline">Outline</Badge>\n      <Badge variant="success">Success</Badge>\n      <Badge variant="warning">Warning</Badge>\n      <Badge variant="destructive">Destructive</Badge>\n      <Badge variant="info">Info</Badge>\n    </div>\n  );\n}'}
>
  <div className="flex flex-wrap gap-2 items-center">
    <Badge variant="default">Default</Badge>
    <Badge variant="secondary">Secondary</Badge>
    <Badge variant="outline">Outline</Badge>
    <Badge variant="success">Success</Badge>
    <Badge variant="warning">Warning</Badge>
    <Badge variant="destructive">Destructive</Badge>
    <Badge variant="info">Info</Badge>
  </div>
</ComponentPreview>

## Sizes

<ComponentPreview
  title="Badge Sizes"
  description="Compact status badges available in xs (16px), sm (18px), default (20px), and lg (24px)."
  code={'import { Badge } from "@galaui/react";\n\nexport default function Example() {\n  return (\n    <div className="flex flex-wrap gap-2.5 items-center">\n      <Badge size="xs">Extra Small (16px)</Badge>\n      <Badge size="sm">Small (18px)</Badge>\n      <Badge size="default">Default (20px)</Badge>\n      <Badge size="lg">Large (24px)</Badge>\n    </div>\n  );\n}'}
>
  <div className="flex flex-wrap gap-2.5 items-center">
    <Badge size="xs">Extra Small (16px)</Badge>
    <Badge size="sm">Small (18px)</Badge>
    <Badge size="default">Default (20px)</Badge>
    <Badge size="lg">Large (24px)</Badge>
  </div>
</ComponentPreview>

## With Icons

<ComponentPreview
  title="Badges with Icons"
  description="Combines icons with badge text for quick recognition."
  code={'import { Badge } from "@galaui/react";\nimport { CheckCircle2, AlertTriangle, Sparkles } from "lucide-react";\n\nexport default function Example() {\n  return (\n    <div className="flex flex-wrap gap-2.5 items-center">\n      <Badge variant="success" className="gap-1">\n        <CheckCircle2 className="w-3 h-3" />\n        <span>Deployed</span>\n      </Badge>\n      <Badge variant="warning" className="gap-1">\n        <AlertTriangle className="w-3 h-3" />\n        <span>Needs Review</span>\n      </Badge>\n      <Badge variant="default" className="gap-1">\n        <Sparkles className="w-3 h-3" />\n        <span>New Feature</span>\n      </Badge>\n    </div>\n  );\n}'}
>
  <div className="flex flex-wrap gap-2.5 items-center">
    <Badge variant="success" className="gap-1">
      <CheckCircle2 className="w-3 h-3" />
      <span>Deployed</span>
    </Badge>
    <Badge variant="warning" className="gap-1">
      <AlertTriangle className="w-3 h-3" />
      <span>Needs Review</span>
    </Badge>
    <Badge variant="default" className="gap-1">
      <Sparkles className="w-3 h-3" />
      <span>New Feature</span>
    </Badge>
  </div>
</ComponentPreview>

## API Reference

<ApiReference component="Badge" />
