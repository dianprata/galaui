import { Collapsible, CollapsibleTrigger, CollapsiblePanel, Button, Badge } from "@/index";
import { ComponentPreview } from "@docs/components/ComponentPreview";
import { CodeBlock } from "@docs/components/CodeBlock";
import { ChevronsUpDown } from "lucide-react";

# Collapsible

An interactive component which expands and collapses a panel of content. Built on Base UI Collapsible primitives.

<div className="flex flex-wrap items-center gap-2 my-4">
  <Badge variant="outline" className="font-mono text-[11px]">@base-ui/react</Badge>
  <Badge variant="secondary" className="font-mono text-[11px]">Smooth Animation</Badge>
</div>

## Import

<CodeBlock language="tsx" code={'import { Collapsible, CollapsibleTrigger, CollapsiblePanel } from "@galaui/react";'} />

## Example

<ComponentPreview
  title="Collapsible Repositories"
  description="Disclosure component with trigger and animated panel."
  code={'import { Collapsible, CollapsibleTrigger, CollapsiblePanel, Button } from "@galaui/react";\nimport { ChevronsUpDown } from "lucide-react";\n\nexport default function Example() {\n  return (\n    <Collapsible className="w-full max-w-sm rounded-xl border border-border bg-card p-3 space-y-2">\n      <div className="flex items-center justify-between">\n        <span className="text-xs font-semibold">@galaui/react (3 repositories)</span>\n        <CollapsibleTrigger>\n          <Button variant="ghost" size="icon-sm">\n            <ChevronsUpDown className="w-3.5 h-3.5 text-muted-foreground" />\n          </Button>\n        </CollapsibleTrigger>\n      </div>\n      <div className="rounded-lg border border-border bg-background px-3 py-1.5 text-xs font-mono">\n        @galaui/core\n      </div>\n      <CollapsiblePanel className="space-y-2">\n        <div className="rounded-lg border border-border bg-background px-3 py-1.5 text-xs font-mono">\n          @galaui/tokens\n        </div>\n        <div className="rounded-lg border border-border bg-background px-3 py-1.5 text-xs font-mono">\n          @galaui/icons\n        </div>\n      </CollapsiblePanel>\n    </Collapsible>\n  );\n}'}
>
  <Collapsible className="w-full max-w-sm rounded-xl border border-border bg-card p-3 space-y-2">
    <div className="flex items-center justify-between">
      <span className="text-xs font-semibold">@galaui/react (3 repositories)</span>
      <CollapsibleTrigger>
        <Button variant="ghost" size="icon-sm">
          <ChevronsUpDown className="w-3.5 h-3.5 text-muted-foreground" />
        </Button>
      </CollapsibleTrigger>
    </div>
    <div className="rounded-lg border border-border bg-background px-3 py-1.5 text-xs font-mono">
      @galaui/core
    </div>
    <CollapsiblePanel className="space-y-2">
      <div className="rounded-lg border border-border bg-background px-3 py-1.5 text-xs font-mono">
        @galaui/tokens
      </div>
      <div className="rounded-lg border border-border bg-background px-3 py-1.5 text-xs font-mono">
        @galaui/icons
      </div>
    </CollapsiblePanel>
  </Collapsible>
</ComponentPreview>
