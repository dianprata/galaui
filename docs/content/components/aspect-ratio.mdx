import { AspectRatio, Badge } from "@/index";
import { ComponentPreview } from "@docs/components/ComponentPreview";
import { CodeBlock } from "@docs/components/CodeBlock";

# Aspect Ratio

Displays content within a desired proportional aspect ratio, preventing cumulative layout shift (CLS).

<div className="flex flex-wrap items-center gap-2 my-4">
  <Badge variant="outline" className="font-mono text-[11px]">CSS AspectRatio</Badge>
  <Badge variant="secondary" className="font-mono text-[11px]">Responsive</Badge>
</div>

## Import

<CodeBlock language="tsx" code={'import { AspectRatio } from "@galaui/react";'} />

## 16:9 Ratio

<ComponentPreview
  title="16:9 Aspect Ratio"
  description="Maintains a 16:9 widescreen proportion."
  code={'import { AspectRatio } from "@galaui/react";\n\nexport default function Example() {\n  return (\n    <div className="w-full max-w-md overflow-hidden rounded-xl border border-border shadow-xs">\n      <AspectRatio ratio={16 / 9} className="bg-gradient-to-br from-indigo-500 via-purple-500 to-pink-500 flex items-center justify-center text-white font-semibold text-sm">\n        16:9 Media Ratio\n      </AspectRatio>\n    </div>\n  );\n}'}
>
  <div className="w-full max-w-md overflow-hidden rounded-xl border border-border shadow-xs">
    <AspectRatio ratio={16 / 9} className="bg-gradient-to-br from-indigo-500 via-purple-500 to-pink-500 flex items-center justify-center text-white font-semibold text-sm">
      16:9 Media Ratio
    </AspectRatio>
  </div>
</ComponentPreview>
