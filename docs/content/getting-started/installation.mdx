import { CodeBlock } from "@docs/components/CodeBlock";
import { ComponentPreview } from "@docs/components/ComponentPreview";
import { Alert, AlertTitle, AlertDescription, Tabs, TabsList, TabsTab, TabsPanel, Separator, Button } from "@/index";
import { CheckCircle2 } from "lucide-react";

# Installation

Install `@galaui/react` and its peer dependencies into your React project.

## 1. Package Installation

Select your package manager to install the core package:

<Tabs defaultValue="npm" className="my-6">
  <TabsList>
    <TabsTab value="npm">npm</TabsTab>
    <TabsTab value="pnpm">pnpm</TabsTab>
    <TabsTab value="yarn">yarn</TabsTab>
    <TabsTab value="bun">bun</TabsTab>
  </TabsList>

  <TabsPanel value="npm">
    <CodeBlock language="bash" code="npm install @galaui/react" />
  </TabsPanel>

  <TabsPanel value="pnpm">
    <CodeBlock language="bash" code="pnpm add @galaui/react" />
  </TabsPanel>

  <TabsPanel value="yarn">
    <CodeBlock language="bash" code="yarn add @galaui/react" />
  </TabsPanel>

  <TabsPanel value="bun">
    <CodeBlock language="bash" code="bun add @galaui/react" />
  </TabsPanel>
</Tabs>

<Separator className="my-8" />

## 2. Import Stylesheet

GalaUI includes all base styles and Tailwind CSS v4 design tokens in `@galaui/react/styles.css`. Import it at the root of your application (e.g. `main.tsx`, `App.tsx`, or Next.js `app/layout.tsx`):

<CodeBlock
  language="tsx"
  code={'import "@galaui/react/styles.css";\nimport React from "react";\nimport ReactDOM from "react-dom/client";\nimport App from "./App";\n\nReactDOM.createRoot(document.getElementById("root")!).render(<App />);'}
/>

<Separator className="my-8" />

## 3. Verify Setup

Render your first GalaUI component to verify that styling and Base UI primitives work:

<ComponentPreview
  title="Verification Button"
  description="Ensure styles and Base UI interactivity are loaded properly."
  code={'import { Button } from "@galaui/react";\n\nexport default function App() {\n  return <Button variant="default">Hello GalaUI</Button>;\n}'}
>
  <Button variant="default">Hello GalaUI</Button>
</ComponentPreview>

<Alert variant="success" className="my-6">
  <CheckCircle2 className="w-4 h-4" />
  <div>
    <AlertTitle>Ready to build!</AlertTitle>
    <AlertDescription>
      Your project is now fully configured with GalaUI. Check out the Theming guide next to customize design tokens.
    </AlertDescription>
  </div>
</Alert>
